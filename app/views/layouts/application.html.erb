<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
  <title><%= content_for?(:title) ? yield(:title) + " - " + NOME_EMPRESA : NOME_EMPRESA %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) + " - " + NOME_EMPRESA : NOME_EMPRESA %>">
  <meta name="author" content="<%= NOME_EMPRESA %>">
  <meta name="theme-color" content="#222222">
  <!-- Meta Tags -->
  <meta property="og:locale" content="pt_BR">

  <!-- Main css styles -->
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>

  <!-- Main javascript files -->
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body id="page-top">

<div id="wrapper">
  <%= render 'layouts/menu_lateral' %>

  <div id="content-wrapper" class="d-flex flex-column">
    <div id="content">
      <%= render "layouts/menu_topo" %>

      <div class="container-fluid">
        <%= render 'layouts/navegacao' %>
        <%= render 'layouts/flash_alerts' %>
        <%= yield %>
      </div>
    </div>

    <%= render 'layouts/footer' %>
  </div>
</div>

<%= yield :javascript %>

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
  <i class="fas fa-angle-up"></i>
</a>

<!-- Logout Modal-->
<div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">

    </div>
  </div>
</div>

<div class="modal fade" id="modal_basic" data-backdrop="static"  tabindex="-1" role="dialog" aria-hidden="true">
  <div class="modal-dialog modal-md">
    <div class="modal-content animated bounceInRight" id="modal_content_basic">
    </div>
  </div>
</div>

</body>
</html>
